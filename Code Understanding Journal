Prompt 1: Understanding Project Structure and Technology Stack

## Me
I'm a junior developer who just joined this project.

Here's my current understanding of the project:
- It seems to be a journal with tasks with levels of importance some are of high priority
others with due date it also seems they have tags to show the completion status - It appears
to use the tools java and gradle -The folder structure seems to follow a folder inside a folder
to last one, its hierarchical Project structure: use-cases\code-comprehension-001\java\TaskManager
\src\main\java\za\co\wethinkcode\taskmanager\app\TaskManager.java Key configuration files:
gradle-wrapper.properties.

The AI Assistant clarified the application concept and provided some additional context:
1) The application is a command-line interface (CLI) application that allows users to manage tasks.
2) It has tags to indicate when a task is completed.
3) It has a priority level to indicate the urgency of the task.
4) It has a due date to indicate when the task needs to be completed.
5) It also has a status level to indicate whether the task is completed or not.
6) It apparently uses JSON files to stiore the data.
7) It is a command-line application and not a mobile or lab application.
8) It also tracks stats.

##Me
So the frameworks used I assume for the file structure is gradle, json. And the tech used is java.
What is the file structure here exactly?

AI Assistant showed that the file structure is a layered architecture.

## **Project File Structure:**
Based on the code provided, the structure follows a standard Maven/Gradle convention with a **layered architecture**:```
project-root/src/main/java/za/co/wethinkcode/taskmanager/app/askManager.java (Business logic layer)
            cli/TaskManagerCli.java       (Command-line interface layer)
            model/Task.java
                TaskPriority.java
                TaskStatus.java           (Data models)
            storage/TaskStorage.java      (Persistence/Data access layer)
    build.gradle                          (Gradle build configuration)
    gradle-wrapper.properties
    tasks.json                            (Data storage file)


Architecture Layers:
CLI Layer (TaskManagerCli.java) - Handles user input and command parsing using Apache Commons CLI
Business Logic (TaskManager.java) - Core application logic for task operations
Model Layer - Data structures (Task, TaskPriority, TaskStatus)
Storage Layer (TaskStorage.java) - Handles JSON file persistence
This is a clean, modular structure that separates concerns effectively!

## Me
1. Identify additional key technologies, frameworks, and libraries used
2. Point out where the application entry points are located
3. Suggest 3-5 specific questions I should ask my team to deepen my understanding

1. Additional Key Technologies, Frameworks, and Libraries

Apache Commons CLI - Command-line argument parsing and option handling
JUnit 5 (Jupiter) - Unit testing framework
Mockito - Mocking framework for unit tests
Java Time API - Date/time handling (LocalDate, LocalDateTime)
Java Streams API - Functional programming for data filtering and transformation
JSON - Data serialization format (tasks.json)


2. Application Entry Points
Primary Entry Point:
File: src/main/java/za/co/wethinkcode/taskmanager/cli/TaskManagerCli.java

Secondary Components:
TaskManager class - Acts as the orchestrator between CLI and storage
TaskStorage class - Handles all persistence operations


3. Suggested Questions to Ask Your Team
Architecture & Design Decisions
"Why does the application store tasks directly in a JSON file rather than using a database like SQLite
or PostgreSQL? Are there plans to scale this to a multi-user system?"
(This reveals future scalability intentions and architectural limitations
"The TaskManager class instantiates TaskStorage directly.)

Error Handling & Validation
"What happens if the tasks.json file becomes corrupted or invalid JSON? Should we add error recovery mechanisms or
rollback functionality?" (This identifies robustness gaps and potential improvements)

I just learned that JSON can store data, these questions regarding the data storage storage structure
are important for me to understand.

## Me
so its a command line application not a web oir mobile application?

## AI Assistant

Yes, exactly correct! This is a command-line (CLI) application, not a web or mobile application.
Key Evidence:
1. Entry Point is CLI-Based
The main entry point is TaskManagerCli.java with a main(String[] args) method
It uses Apache Commons CLI library for parsing command-line arguments and options
All user interaction happens through terminal/console commands


Prompt 2: Finding Feature Implementation Locations
## Me
Question i see a function called load. Is it perhaps used for loading
the data on the file on the program? If not are there any external
files loaded, where exactly?


AI Assistant: The assistant confirmed that the load() function does indeed load json data into
the memory of the program.
The JSON file is the external file, it contains the tasks data in JSON format.
It also updates everytime a task is created, modified or deleted.

Data export is also in the TaskStorage.java class, through the save() function the data is
written back to the JSON file.

load() is used to load data from the JSON file into memory.

Note which existing components might need to be modified:
TaskStorage.java - Core persistence logic
TaskManagerCli.java - User interface/commands
TaskManager.java - Business logic enhancements
Task.java - New properties/methods
TaskManagerTest.java - Testing new features
build.gradle - Only if adding new libraries
TaskPriority.java - Stable enum
TaskStatus.java - Stable enum
The storage and CLI layers would be modified most frequently as they handle data persistence
and user interaction.

I was asked to add a  "Task Export to CSV" feature, but I'm not sure where the code for this feature lives.
I'll help you locate where the "Task Export to CSV" feature should be implemented.
Let me first explore the project structure to get a better understanding of the codebase.
Based on my investigation, the **"Task Export to CSV" feature doesn't exist yet** in your codebase.
This is a new feature you need to add.

## Where to Add CSV Export
### 1. **Storage Layer** (Primary Location)
Add the CSV export method to : `TaskStorage.java`
- This is the logical place since it handles all data persistence

### 2. **Business Logic Layer** (Wrapper)
Add a corresponding method to : `TaskManager.java`
- This acts as a wrapper around the storage layer

### 3. **CLI Layer** (User Interface)
Add a new command handler to : `TaskManagerCli.java`
- Add an `export` command that users can call from the terminal

## Recommended Implementation Path
1. **Start with `TaskStorage.java`** - implement the core CSV writing logic
2. **Add to `TaskManager.java`** - create a public wrapper method
3. **Update `TaskManagerCli.java`** - add the CLI command handler and help text
4. **Write tests in `TaskManagerTest.java`** - test the new functionality


TaskStorage
writeCsvFile(String filePath)
Handles file I/O and CSV formatting. Accesses all tasks from the tasks map.

TaskManager
exportTasksToCsv(String filePath)
Wrapper method that calls storage layer. Keeps business logic organized.

TaskManagerCli
handleExportCommand(String[] args)
User interface. Parses arguments, calls TaskManager, gives feedback.

TaskManagerTest
test_exportTasksToCsv_*()
Unit tests for the feature (success cases, error handling, edge cases).

Users can now save their tasks in a format that spreadsheet applications understand,
making it easier to share, print, and analyze task data.

✅ Extract to CsvExporter class - Keeps code cleaner and more testable
✅ Add format filtering - Users want to export specific subsets of tasks
✅ Better error feedback - Show how many tasks were exported + clear error messages

Prompt 3: Understanding Domain Models and Business Concepts
The core entity classes are: TaskStorage: Responsible for storing the tasks recorded in the program.
TaskStatus: Will keep track how the task status changes
TaskPriority: Responsible for tracking task priority
Task: Shows the task
TaskManagerCli: Manages the tasks and user interaction.

What does the task class do ?
Simple Answer
The Task class is like a "Post-it Note" - it holds all the information about a single to-do item
and provides ways to modify and query that information. The TaskManager uses it to create, update,
and manage all your tasks.


I'd like you to act as a senior developer who deeply understands our codebase's domain model.
I'm a junior developer trying to make sense of the business logic and domain concepts in this
application.

Here's what I've found in the codebase:
```
Good! You've correctly identified the core entities. Let me provide you with a senior developer's
perspective on the domain model and help you understand the business logic flow.

Domain Model Deep Dive
1. The Domain Concepts (Business Reality)
Before we talk about code, let's understand what we're modeling:
Core Concept: A Task (Work Item)
In the real world, a
task is a unit of work that needs to be done. It has:
Identity - A unique ID to distinguish it from other tasks
Description - What needs to be done (title + details)
Urgency - How important/critical it is (priority)
Progress - Where it stands in its lifecycle (status)
Timeline - When it should be finished (due date)
Organization - How to categorize it (tags)
Timestamps - When it was created and completed

Key Question: "What are we storing and managing?" Answer: Tasks

Key Question: "What are the allowed states a task can be in?"
Answer: Defined by these enums - no invalid states allowed

Key Question: "How do we persist tasks between sessions?"
Answer: Serialize them to JSON and deserialize on startup


CLI
Parse commands, format output
How to talk to TaskManager
Database, JSON

TaskManager
Validate input, apply rules
How to query/update tasks
Files, serialization

TaskStorage
Persist data, retrieve data
JSON format, HashMap
Business logic, UI

Task
Hold data, report properties

You've identified the architectural layers properly:
TaskStorage = Data access layer
TaskStatus/TaskPriority = Business constraints
Task = Domain entity
TaskManager = Business logic orchestrator
TaskManagerCli = User interface
No misconceptions. You're thinking about this correctly

The domain has these concepts:

Task: A unit of work
Progress/Status: Where in lifecycle
Urgency/Priority: Business impact
Deadline/Due Date: Time constraint
Labels/Tags: Organization
Timestamps: Accountability

Key insight: These aren't just data fields—they enforce business workflows.

Domain Patterns You're Using
Pattern 1: Aggregate Root
Task is the aggregate root—all changes go through it.

Pattern 2: Value Objects
TaskStatus, TaskPriority are enums = value objects with no identity.

Pattern 3: Persistence Ignorance
Task doesn't know about JSON files. TaskStorage handles that.

Domain-Specific Terminology

updateTaskStatus()  Advance task through lifecycle
markAsDone() Special action: mark complete + record time
isOverdue() Business rule: due date < now
getStatistics() Metrics: productivity measurements
Priority enum Defines work urgency hierarchy
Misconception to avoid: These aren't random methods—they model real business workflows.

Missing: Domain Events
When a task is marked DONE, you should trigger:
"TaskCompleted" event
Update completion timestamp
Notify user
Currently just sets status. Clean but basic.

his is what "domain modeling" means: Understanding the real-world behavior you're codifying.